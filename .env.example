NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional. Minutes of inactivity before a user is signed out. Defaults to 30.
# SESSION_IDLE_TIMEOUT_MINUTES=30

# Optional. Restricts sign-in to these email domains, comma-separated:
#   ALLOWED_EMAIL_DOMAINS=company.co.th,subsidiary.com
# Leave unset (the default) to allow any Google account.
ALLOWED_EMAIL_DOMAINS=
